# Arquivo para testes do desafio4
from desafio4 import Change

change = Change(25,40)
change.show_change()

change = Change(35.80,50)
change.show_change()

change = Change(40,40)
change.show_change()

change = Change(0.05,100)
change.show_change()